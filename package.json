{
  "name": "almi-goethe",
  "version": "0.1.0",
  "private": true,
  "description": "AlmiGoethe — AI-powered Goethe-Zertifikat German practice in the AlmiWorld family. Four modules (Lesen, Hören, Schreiben, Sprechen) at six CEFR levels (A1–C2), scored per module as a points estimate with a 60% pass mark, no fabricated composite. Separate product (almigoethe.almiworld.com). Original German content — never copied from the Goethe-Institut. Practice estimates only; confirm the level you need with the Goethe-Institut.",
  "scripts": {
    "dev": "next dev",
    "build": "node scripts/seo/uniqueness-gate.mjs && prisma generate && prisma migrate deploy && next build && npm run seed:prod",
    "seed:prod": "(tsx scripts/seed/append.ts && tsx scripts/seed/exam-append.ts) || echo 'seed:prod skipped (non-fatal)'",
    "start": "next start",
    "postinstall": "prisma generate",
    "seed:lesen": "tsx scripts/seed/lesen.ts",
    "seed:hoeren": "tsx scripts/seed/hoeren.ts",
    "seed:schreiben": "tsx scripts/seed/schreiben.ts",
    "seed:sprechen": "tsx scripts/seed/sprechen.ts",
    "seed:all": "npm run seed:lesen && npm run seed:hoeren && npm run seed:schreiben && npm run seed:sprechen",
    "seed:append": "tsx scripts/seed/append.ts",
    "seed:exams": "tsx scripts/seed/exam-append.ts",
    "validate:seed": "tsx scripts/validate-seed.ts",
    "validate:exam-seed": "tsx scripts/validate-exam-seed.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.91.1",
    "@prisma/client": "^6.19.3",
    "@smnasiruz016-blip/almi-data": "^0.4.0",
    "bcryptjs": "^3.0.3",
    "next": "16.2.6",
    "prisma": "^6.19.3",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "resend": "^6.12.3",
    "stripe": "^22.1.0",
    "tsx": "^4.21.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
